/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['i.ibb.co'], // Add your allowed external image domains here
      },
}

module.exports = nextConfig
